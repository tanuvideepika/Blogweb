<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogweb</title>
    <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>
<body>
    <%- include("header.ejs") %>
    <div class="bloggum">
    <% if (locals.n==0) { %>
     <h2>No blogs found. Try creating one!</h2>
    <% } else {%>
        
            <% for( let i = 0; i < n; i++ ) { %>
                <div class="blog-card">
                    <h2 class="card-title"><%= blog[i].title %></h2>
                    <p class="card-description"><%= blog[i].description %></p>
                  </div>
            <% } %>
            
              
              
        
        
    <% } %>
</div>
    <%- include("footer.ejs") %>
</body>
</html>